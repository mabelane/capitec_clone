import 'package:flutter/material.dart';

class MoneyOut extends StatelessWidget {
  const MoneyOut({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
