import 'package:flutter/material.dart';

class LiveBetterAccount extends StatelessWidget {
  const LiveBetterAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
